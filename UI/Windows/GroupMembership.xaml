<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Name="winGroupMembership" Title="Group Membership" Height="580" Width="700"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResize" WindowStyle="SingleBorderWindow"
        Background="{Theme_Bg}" FontFamily="Segoe UI Variable Display, Segoe UI, sans-serif">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Bd" Property="Opacity" Value="0.8"/></Trigger>
                            <Trigger Property="IsEnabled" Value="False"><Setter TargetName="Bd" Property="Opacity" Value="0.4"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{Theme_BtnBg}"/>
            <Setter Property="Foreground" Value="{Theme_Fg}"/>
            <Setter Property="BorderBrush" Value="{Theme_BtnBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="GridViewColumnHeader">
            <Setter Property="Background" Value="{Theme_Bg}"/>
            <Setter Property="Foreground" Value="{Theme_SecFg}"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="{Theme_GridBorder}"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{Theme_Bg}" Padding="16" BorderBrush="{Theme_GridBorder}" BorderThickness="0,0,0,1">
            <Grid>
                <StackPanel>
                    <TextBlock x:Name="lblHeaderTitle" Text="Member Of" FontWeight="SemiBold" FontSize="16" Foreground="{Theme_Fg}"/>
                    <TextBlock x:Name="lblHeaderSub" Text="" FontSize="12" Foreground="{Theme_SecFg}" Margin="0,3,0,0"/>
                </StackPanel>
                <Button x:Name="btnRemoveGroup" Content="Remove from Selected Group"
                        HorizontalAlignment="Right" VerticalAlignment="Center"
                        Height="30" Padding="14,0"
                        Background="{Theme_Danger}" Foreground="#FFFFFF" BorderThickness="0"
                        IsEnabled="False"/>
            </Grid>
        </Border>

        <!-- Current group membership list -->
        <Border Grid.Row="1" Margin="8,8,8,4" Background="{Theme_Bg}" CornerRadius="6" BorderBrush="{Theme_GridBorder}" BorderThickness="1">
            <ListView x:Name="lvGroups" BorderThickness="0" Margin="4" Background="Transparent" Foreground="{Theme_Fg}" AlternationCount="2">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Height" Value="28"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="{Theme_Fg}"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListViewItem">
                                    <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="Transparent" BorderThickness="0" CornerRadius="4" Padding="4,0" Margin="0,1">
                                        <GridViewRowPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="true">
                                            <Setter TargetName="Bd" Property="Background" Value="{Theme_PrimaryBg}"/>
                                            <Setter Property="Foreground" Value="{Theme_PrimaryFg}"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="true">
                                            <Setter TargetName="Bd" Property="Background" Value="{Theme_HoverBg}"/>
                                        </Trigger>
                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="true"/>
                                                <Condition Property="IsMouseOver" Value="true"/>
                                            </MultiTrigger.Conditions>
                                            <Setter TargetName="Bd" Property="Background" Value="{Theme_PrimaryBg}"/>
                                            <Setter Property="Foreground" Value="{Theme_PrimaryFg}"/>
                                        </MultiTrigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                                <Setter Property="Background" Value="{Theme_AltRowBg}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Name"     DisplayMemberBinding="{Binding Name}"          Width="280"/>
                        <GridViewColumn Header="Scope"    DisplayMemberBinding="{Binding GroupScope}"    Width="110"/>
                        <GridViewColumn Header="Category" DisplayMemberBinding="{Binding GroupCategory}" Width="110"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>

        <!-- Add to Group section -->
        <Border Grid.Row="2" Margin="8,4,8,4" Padding="12,10" Background="{Theme_BtnBg}" CornerRadius="6" BorderBrush="{Theme_GridBorder}" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="txtAddGroup" Grid.Column="0" Height="30"
                         ToolTip="Type part of a group name and click Search, then select a result to add"/>
                <Button x:Name="btnSearchGroup" Grid.Column="2" Content="Search Groups"
                        Height="30" Padding="14,0"
                        Background="{Theme_BtnBg}" Foreground="{Theme_Fg}" BorderBrush="{Theme_BtnBorder}" BorderThickness="1"/>
                <Button x:Name="btnAddGroup" Grid.Column="4" Content="Add to Group"
                        Height="30" Padding="14,0" IsEnabled="False"
                        Background="{Theme_PrimaryBg}" Foreground="{Theme_PrimaryFg}" BorderThickness="0"/>
            </Grid>
        </Border>

        <!-- Search results for Add-to-Group -->
        <Border Grid.Row="3" x:Name="borderSearchResults" Margin="8,0,8,4"
                Background="{Theme_Bg}" CornerRadius="6" BorderBrush="{Theme_GridBorder}" BorderThickness="1"
                MaxHeight="140" Visibility="Collapsed">
            <ListBox x:Name="lbGroupSearch" BorderThickness="0" Background="Transparent"
                     Foreground="{Theme_Fg}" FontSize="13" MaxHeight="130">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="8,5"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="3" Margin="2,1" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="{Theme_PrimaryBg}"/>
                                            <Setter Property="Foreground" Value="{Theme_PrimaryFg}"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="{Theme_HoverBg}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
        </Border>

        <!-- Footer -->
        <Border Grid.Row="4" x:Name="borderFooter" Background="{Theme_BtnBg}" BorderBrush="{Theme_GridBorder}" BorderThickness="0,1,0,0" Padding="16,10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock x:Name="lblGrpStatus" Text="" Foreground="{Theme_SecFg}" FontSize="12" VerticalAlignment="Center" Margin="0,0,16,0"/>
                <Button x:Name="btnClose" Content="Close" Width="80" Height="28"
                        Background="{Theme_BtnBg}" Foreground="{Theme_Fg}" BorderBrush="{Theme_BtnBorder}" BorderThickness="1"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>