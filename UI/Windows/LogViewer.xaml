<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Event Log Viewer" Height="620" Width="960" WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize" WindowStyle="SingleBorderWindow" Background="{Theme_Bg}"
        FontFamily="Segoe UI Variable Display, Segoe UI, sans-serif">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Bd" Property="Opacity" Value="0.8"/></Trigger>
                            <Trigger Property="IsEnabled" Value="False"><Setter TargetName="Bd" Property="Opacity" Value="0.4"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{Theme_BtnBg}"/>
            <Setter Property="Foreground" Value="{Theme_Fg}"/>
            <Setter Property="BorderBrush" Value="{Theme_BtnBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="GridViewColumnHeader">
            <Setter Property="Background" Value="{Theme_Bg}"/>
            <Setter Property="Foreground" Value="{Theme_SecFg}"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="{Theme_GridBorder}"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header bar -->
        <Border Grid.Row="0" Background="{Theme_Bg}" Padding="16" BorderBrush="{Theme_GridBorder}" BorderThickness="0,0,0,1">
            <Grid>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Event Log Viewer" FontWeight="SemiBold" FontSize="16" Foreground="{Theme_Fg}"/>
                    <TextBlock Text="Filter by operator, target user, or date range." FontSize="12" Foreground="{Theme_SecFg}" Margin="0,3,0,0"/>
                </StackPanel>
                <Button x:Name="btnExport" Content="Export CSV" HorizontalAlignment="Right"
                        Width="100" Height="28" Background="{Theme_BtnBg}" Foreground="{Theme_Fg}"
                        BorderBrush="{Theme_BtnBorder}" BorderThickness="1"/>
            </Grid>
        </Border>

        <!-- Content area -->
        <Grid Grid.Row="1" Margin="12,12,12,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Margin="0,0,0,10" Padding="14,12" Background="{Theme_BtnBg}" CornerRadius="6" BorderBrush="{Theme_GridBorder}" BorderThickness="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="180"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="180"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0"  Text="Operator"    FontWeight="SemiBold" Foreground="{Theme_SecFg}" FontSize="13" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBox   x:Name="txtFilterOperator" Grid.Column="1"  Height="28"/>
                    <TextBlock Grid.Column="3"  Text="Target User" FontWeight="SemiBold" Foreground="{Theme_SecFg}" FontSize="13" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBox   x:Name="txtFilterUser"     Grid.Column="4"  Height="28"/>
                    <TextBlock Grid.Column="6"  Text="Start Date"  FontWeight="SemiBold" Foreground="{Theme_SecFg}" FontSize="13" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <DatePicker x:Name="dpStartDate"      Grid.Column="7"  Height="28" VerticalContentAlignment="Center"/>
                    <TextBlock Grid.Column="9"  Text="End Date"    FontWeight="SemiBold" Foreground="{Theme_SecFg}" FontSize="13" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <DatePicker x:Name="dpEndDate"        Grid.Column="10" Height="28" VerticalContentAlignment="Center"/>
                    <Button x:Name="btnFilter" Grid.Column="12" Content="Apply Filter"
                            Height="28" Padding="14,0" Background="{Theme_PrimaryBg}" Foreground="{Theme_PrimaryFg}" BorderThickness="0"/>
                </Grid>
            </Border>

            <!-- Results list -->
            <Border Grid.Row="1" Background="{Theme_Bg}" CornerRadius="6" BorderBrush="{Theme_GridBorder}" BorderThickness="1">
                <ListView x:Name="lvLogs" BorderThickness="0" Margin="4" Background="Transparent" Foreground="{Theme_Fg}" AlternationCount="2">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Height" Value="28"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="{Theme_Fg}"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListViewItem">
                                        <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="Transparent" BorderThickness="0" CornerRadius="4" Padding="4,0" Margin="0,1">
                                            <GridViewRowPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="true">
                                                <Setter TargetName="Bd" Property="Background" Value="{Theme_PrimaryBg}"/>
                                                <Setter Property="Foreground" Value="{Theme_PrimaryFg}"/>
                                            </Trigger>
                                            <Trigger Property="IsMouseOver" Value="true">
                                                <Setter TargetName="Bd" Property="Background" Value="{Theme_HoverBg}"/>
                                            </Trigger>
                                            <MultiTrigger>
                                                <MultiTrigger.Conditions>
                                                    <Condition Property="IsSelected" Value="true"/>
                                                    <Condition Property="IsMouseOver" Value="true"/>
                                                </MultiTrigger.Conditions>
                                                <Setter TargetName="Bd" Property="Background" Value="{Theme_PrimaryBg}"/>
                                                <Setter Property="Foreground" Value="{Theme_PrimaryFg}"/>
                                            </MultiTrigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                                    <Setter Property="Background" Value="{Theme_AltRowBg}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Timestamp"   DisplayMemberBinding="{Binding Timestamp}" Width="140"/>
                            <GridViewColumn Header="Event"       DisplayMemberBinding="{Binding Event}"     Width="130"/>
                            <GridViewColumn Header="Target User" DisplayMemberBinding="{Binding Username}"  Width="120"/>
                            <GridViewColumn Header="Details"     DisplayMemberBinding="{Binding Details}"   Width="260"/>
                            <GridViewColumn Header="Status"      DisplayMemberBinding="{Binding Status}"    Width="80"/>
                            <GridViewColumn Header="Operator"    DisplayMemberBinding="{Binding Operator}"  Width="100"/>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Border>
        </Grid>

        <!-- Footer -->
        <Border Grid.Row="2" Background="{Theme_BtnBg}" BorderBrush="{Theme_GridBorder}" BorderThickness="0,1,0,0" Padding="16,10">
            <Button x:Name="btnCloseLog" Content="Close" Width="80" Height="28" HorizontalAlignment="Right"
                    Background="{Theme_BtnBg}" Foreground="{Theme_Fg}" BorderBrush="{Theme_BtnBorder}" BorderThickness="1"/>
        </Border>
    </Grid>
</Window>
