<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Name="winPrinterManager" Title="Printer Management" Height="620" Width="960"
        WindowStartupLocation="CenterScreen" ResizeMode="CanResize" WindowStyle="SingleBorderWindow"
        Background="{Theme_Bg}" FontFamily="Segoe UI Variable Display, Segoe UI, sans-serif">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Bd" Property="Opacity" Value="0.8"/></Trigger>
                            <Trigger Property="IsEnabled" Value="False"><Setter TargetName="Bd" Property="Opacity" Value="0.5"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ListViewItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Margin" Value="0,0,0,2"/>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Padding="15" BorderBrush="{Theme_GridBorder}" BorderThickness="0,0,0,1">
            <Grid>
                <StackPanel>
                    <TextBlock Text="Printer Manager" FontSize="18" FontWeight="SemiBold" Foreground="{Theme_Fg}"/>
                    <TextBlock x:Name="lblStatus" Text="Loading printers..." FontSize="12" Foreground="{Theme_SecFg}"/>
                </StackPanel>
                <Button x:Name="btnInstallRemote" Content="Install New Printer..." HorizontalAlignment="Right" VerticalAlignment="Center" Padding="15,6" Background="{Theme_PrimaryBg}" Foreground="{Theme_PrimaryFg}" BorderThickness="0"/>
            </Grid>
        </Border>

        <!-- List View -->
        <Border Grid.Row="1" Margin="10">
            <ListView x:Name="lvPrinters" Background="Transparent" BorderThickness="0" Foreground="{Theme_Fg}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="250"/>
                        <GridViewColumn Header="Driver" DisplayMemberBinding="{Binding DriverName}" Width="250"/>
                        <GridViewColumn Header="Port" DisplayMemberBinding="{Binding PortName}" Width="150"/>
                        <GridViewColumn Header="Location" DisplayMemberBinding="{Binding Location}" Width="150"/>
                        <GridViewColumn Header="Default" DisplayMemberBinding="{Binding Default}" Width="80"/>
                    </GridView>
                </ListView.View>
                <ListView.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="View Details" x:Name="ctxPrnDetails">
                            <MenuItem.Icon>
                                <TextBlock FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" Text="&#xE946;" FontSize="14"/>
                            </MenuItem.Icon>
                        </MenuItem>
                        <MenuItem Header="Rename Printer" x:Name="ctxPrnRename">
                            <MenuItem.Icon>
                                <TextBlock FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" Text="&#xE70F;" FontSize="14"/>
                            </MenuItem.Icon>
                        </MenuItem>
                        <MenuItem Header="Remove Printer" x:Name="ctxPrnRemove">
                            <MenuItem.Icon>
                                <TextBlock FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" Text="&#xE74D;" FontSize="14"/>
                            </MenuItem.Icon>
                        </MenuItem>
                    </ContextMenu>
                </ListView.ContextMenu>
            </ListView>
        </Border>

        <!-- Footer -->
        <Border Grid.Row="2" Background="{Theme_BtnBg}" BorderBrush="{Theme_GridBorder}" BorderThickness="0,1,0,0" Padding="12">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="btnRefresh"    Content="Refresh"        Margin="0,0,8,0" Width="90"  Height="28" Background="{Theme_BtnBg}" Foreground="{Theme_Fg}"     BorderBrush="{Theme_BtnBorder}" BorderThickness="1"/>
                <Button x:Name="btnPrnDetails" Content="View Details"   Margin="0,0,8,0" Width="100" Height="28" Background="{Theme_BtnBg}" Foreground="{Theme_Fg}"     BorderBrush="{Theme_BtnBorder}" BorderThickness="1"/>
                <Button x:Name="btnPrnRemove"  Content="Remove Printer" Margin="0,0,8,0" Width="110" Height="28" Background="{Theme_BtnBg}" Foreground="{Theme_Danger}" BorderBrush="{Theme_BtnBorder}" BorderThickness="1"/>
                <Button x:Name="btnClose"      Content="Close"                           Width="80"  Height="28" Background="{Theme_BtnBg}" Foreground="{Theme_Fg}"     BorderBrush="{Theme_BtnBorder}" BorderThickness="1" IsCancel="True"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>